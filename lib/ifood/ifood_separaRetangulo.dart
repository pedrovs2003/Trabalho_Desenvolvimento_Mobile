import 'package:flutter/material.dart';

//Separa itens das páginas

class SeparaRetangulo extends StatelessWidget {
  const SeparaRetangulo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
